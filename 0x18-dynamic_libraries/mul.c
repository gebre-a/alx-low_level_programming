#include<stdio.h>
#include<stdlib.h>
/**
 * mul - multiplication of two numbers
 * @a: first number
 * @b: second number
 * Return: products
 * for gebre-a project
 */
int mul(int a, int b)
{
	return (a * b);
}
