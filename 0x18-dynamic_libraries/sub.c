#include<stdio.h>
#include<stdlib.h>
/**
 * subtract - subtraction of two numbers
 * @a: first number
 * @b: second number
 * Return: difference
 * for gebre-a project
 */
int sub(int a, int b)
{
	return (a - b);
}
