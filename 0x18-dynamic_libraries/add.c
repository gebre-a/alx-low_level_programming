#include<stdio.h>
#include<stdlib.h>
/**
 * add - addition of two numbers
 * @a: first number
 * @b: second number
 * Return: Addition
 * for gebre-a project
 */
int add(int a, int b)
{
	return (a + b);
}
